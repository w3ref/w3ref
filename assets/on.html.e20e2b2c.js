import{_ as a}from"./plugin-vue_export-helper.21dcd24c.js";import{r as d,o as l,c as s,a as e,d as i,g as o,b as t}from"./app.674943b1.js";const c={},r=o(`<h1 id="x-on" tabindex="-1"><a class="header-anchor" href="#x-on" aria-hidden="true">#</a> x-on</h1><p><code>x-on</code> allows you to easily run code on dispatched DOM events.</p><p>Here&#39;s an example of simple button that shows an alert when clicked.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button x-on:click=&quot;alert(&#39;Hello World!&#39;)&quot;&gt;Say Hi&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p><code>x-on</code> can only listen for events with lower case names, as HTML attributes are case-insensitive. Writing <code>x-on:CLICK</code> will listen for an event named <code>click</code>. If you need to listen for a custom event with a camelCase name, you can use the <a href="#camel"><code>.camel</code> helper</a> to work around this limitation. Alternatively, you can use <a href="./directives/bind#bind-directives"><code>x-bind</code></a> to attach an <code>x-on</code> directive to an element in javascript code (where case will be preserved).</p></blockquote><p><a name="shorthand-syntax"></a></p><h2 id="shorthand-syntax" tabindex="-1"><a class="header-anchor" href="#shorthand-syntax" aria-hidden="true">#</a> Shorthand syntax</h2><p>If <code>x-on:</code> is too verbose for your tastes, you can use the shorthand syntax: <code>@</code>.</p><p>Here&#39;s the same component as above, but using the shorthand syntax instead:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click=&quot;alert(&#39;Hello World!&#39;)&quot;&gt;Say Hi&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="the-event-object"></a></p><h2 id="the-event-object" tabindex="-1"><a class="header-anchor" href="#the-event-object" aria-hidden="true">#</a> The event object</h2><p>If you wish to access the native JavaScript event object from your expression, you can use Alpine&#39;s magic <code>$event</code> property.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click=&quot;alert($event.target.getAttribute(&#39;message&#39;))&quot; message=&quot;Hello World&quot;&gt;Say Hi&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In addition, Alpine also passes the event object to any methods referenced without trailing parenthesis. For example:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click=&quot;handleClick&quot;&gt;...&lt;/button&gt;

&lt;script&gt;
    function handleClick(e) {
        // Now you can access the event object (e) directly
    }
&lt;/script&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="keyboard-events"></a></p><h2 id="keyboard-events" tabindex="-1"><a class="header-anchor" href="#keyboard-events" aria-hidden="true">#</a> Keyboard events</h2><p>Alpine makes it easy to listen for <code>keydown</code> and <code>keyup</code> events on specific keys.</p><p>Here&#39;s an example of listening for the <code>Enter</code> key inside an input element.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input type=&quot;text&quot; @keyup.enter=&quot;alert(&#39;Submitted!&#39;)&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can also chain these key modifiers to achieve more complex listeners.</p><p>Here&#39;s a listener that runs when the <code>Shift</code> key is held and <code>Enter</code> is pressed, but not when <code>Enter</code> is pressed alone.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input type=&quot;text&quot; @keyup.shift.enter=&quot;alert(&#39;Submitted!&#39;)&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,24),u=t("You can directly use any valid key names exposed via "),p={href:"https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values",target:"_blank",rel:"noopener noreferrer"},h=e("code",null,"KeyboardEvent.key",-1),v=t(" as modifiers by converting them to kebab-case."),m=o(`<div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input type=&quot;text&quot; @keyup.page-down=&quot;alert(&#39;Submitted!&#39;)&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>For easy reference, here is a list of common keys you may want to listen for.</p><table><thead><tr><th>Modifier</th><th>Keyboard Key</th></tr></thead><tbody><tr><td><code>.shift</code></td><td>Shift</td></tr><tr><td><code>.enter</code></td><td>Enter</td></tr><tr><td><code>.space</code></td><td>Space</td></tr><tr><td><code>.ctrl</code></td><td>Ctrl</td></tr><tr><td><code>.cmd</code></td><td>Cmd</td></tr><tr><td><code>.meta</code></td><td>Cmd on Mac, Ctrl on Windows</td></tr><tr><td><code>.alt</code></td><td>Alt</td></tr><tr><td><code>.up</code> <code>.down</code> <code>.left</code> <code>.right</code></td><td>Up/Down/Left/Right arrows</td></tr><tr><td><code>.escape</code></td><td>Escape</td></tr><tr><td><code>.tab</code></td><td>Tab</td></tr><tr><td><code>.caps-lock</code></td><td>Caps Lock</td></tr><tr><td><code>.equal</code></td><td>Equal, <code>=</code></td></tr><tr><td><code>.period</code></td><td>Period, <code>.</code></td></tr><tr><td><code>.slash</code></td><td>Foward Slash, <code>/</code></td></tr></tbody></table><p><a name="custom-events"></a></p><h2 id="custom-events" tabindex="-1"><a class="header-anchor" href="#custom-events" aria-hidden="true">#</a> Custom events</h2><p>Alpine event listeners are a wrapper for native DOM event listeners. Therefore, they can listen for ANY DOM event, including custom events.</p><p>Here&#39;s an example of a component that dispatches a custom DOM event and listens for it as well.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div x-data @foo=&quot;alert(&#39;Button Was Clicked!&#39;)&quot;&gt;
	&lt;button @click=&quot;$event.target.dispatchEvent(new CustomEvent(&#39;foo&#39;, { bubbles: true }))&quot;&gt;...&lt;/button&gt;
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When the button is clicked, the <code>@foo</code> listener will be called.</p><p>Because the <code>.dispatchEvent</code> API is verbose, Alpine offers a <code>$dispatch</code> helper to simplify things.</p><p>Here&#39;s the same component re-written with the <code>$dispatch</code> magic property.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div x-data @foo=&quot;alert(&#39;Button Was Clicked!&#39;)&quot;&gt;
  &lt;button @click=&quot;$dispatch(&#39;foo&#39;)&quot;&gt;...&lt;/button&gt;
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a href="./magics/dispatch">\u2192 Read more about <code>$dispatch</code></a></p><p><a name="modifiers"></a></p><h2 id="modifiers" tabindex="-1"><a class="header-anchor" href="#modifiers" aria-hidden="true">#</a> Modifiers</h2><p>Alpine offers a number of directive modifiers to customize the behavior of your event listeners.</p><p><a name="prevent"></a></p><h3 id="prevent" tabindex="-1"><a class="header-anchor" href="#prevent" aria-hidden="true">#</a> .prevent</h3><p><code>.prevent</code> is the equivalent of calling <code>.preventDefault()</code> inside a listener on the browser event object.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;form @submit.prevent=&quot;console.log(&#39;submitted&#39;)&quot; action=&quot;/foo&quot;&gt;
    &lt;button&gt;Submit&lt;/button&gt;
&lt;/form&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the above example, with the <code>.prevent</code>, clicking the button will NOT submit the form to the <code>/foo</code> endpoint. Instead, Alpine&#39;s listener will handle it and &quot;prevent&quot; the event from being handled any further.</p><p><a name="stop"></a></p><h3 id="stop" tabindex="-1"><a class="header-anchor" href="#stop" aria-hidden="true">#</a> .stop</h3><p>Similar to <code>.prevent</code>, <code>.stop</code> is the equivalent of calling <code>.stopPropagation()</code> inside a listener on the browser event object.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @click=&quot;console.log(&#39;I will not get logged&#39;)&quot;&gt;
    &lt;button @click.stop&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the above example, clicking the button WON&#39;T log the message. This is because we are stopping the propagation of the event immediately and not allowing it to &quot;bubble&quot; up to the <code>&lt;div&gt;</code> with the <code>@click</code> listener on it.</p><p><a name="outside"></a></p><h3 id="outside" tabindex="-1"><a class="header-anchor" href="#outside" aria-hidden="true">#</a> .outside</h3><p><code>.outside</code> is a convenience helper for listening for a click outside of the element it is attached to. Here&#39;s a simple dropdown component example to demonstrate:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div x-data=&quot;{ open: false }&quot;&gt;
    &lt;button @click=&quot;open = ! open&quot;&gt;Toggle&lt;/button&gt;

    &lt;div x-show=&quot;open&quot; @click.outside=&quot;open = false&quot;&gt;
        Contents...
    &lt;/div&gt;
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the above example, after showing the dropdown contents by clicking the &quot;Toggle&quot; button, you can close the dropdown by clicking anywhere on the page outside the content.</p><p>This is because <code>.outside</code> is listening for clicks that DON&#39;T originate from the element it&#39;s registered on.</p><blockquote><p>It&#39;s worth noting that the <code>.outside</code> expression will only be evaluated when the element it&#39;s registered on is visible on the page. Otherwise, there would be nasty race conditions where clicking the &quot;Toggle&quot; button would also fire the <code>@click.outside</code> handler when it is not visible.</p></blockquote><p><a name="window"></a></p><h3 id="window" tabindex="-1"><a class="header-anchor" href="#window" aria-hidden="true">#</a> .window</h3><p>When the <code>.window</code> modifier is present, Alpine will register the event listener on the root <code>window</code> object on the page instead of the element itself.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @keyup.escape.window=&quot;...&quot;&gt;...&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The above snippet will listen for the &quot;escape&quot; key to be pressed ANYWHERE on the page.</p><p>Adding <code>.window</code> to listeners is extremely useful for these sorts of cases where a small part of your markup is concerned with events that take place on the entire page.</p><p><a name="document"></a></p><h3 id="document" tabindex="-1"><a class="header-anchor" href="#document" aria-hidden="true">#</a> .document</h3><p><code>.document</code> works similarly to <code>.window</code> only it registers listeners on the <code>document</code> global, instead of the <code>window</code> global.</p><p><a name="once"></a></p><h3 id="once" tabindex="-1"><a class="header-anchor" href="#once" aria-hidden="true">#</a> .once</h3><p>By adding <code>.once</code> to a listener, you are ensuring that the handler is only called ONCE.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click.once=&quot;console.log(&#39;I will only log once&#39;)&quot;&gt;...&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="debounce"></a></p><h3 id="debounce" tabindex="-1"><a class="header-anchor" href="#debounce" aria-hidden="true">#</a> .debounce</h3><p>Sometimes it is useful to &quot;debounce&quot; an event handler so that it only is called after a certain period of inactivity (250 milliseconds by default).</p><p>For example if you have a search field that fires network requests as the user types into it, adding a debounce will prevent the network requests from firing on every single keystroke.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input @input.debounce=&quot;fetchResults&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now, instead of calling <code>fetchResults</code> after every keystroke, <code>fetchResults</code> will only be called after 250 milliseconds of no keystrokes.</p><p>If you wish to lengthen or shorten the debounce time, you can do so by trailing a duration after the <code>.debounce</code> modifier like so:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input @input.debounce.500ms=&quot;fetchResults&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now, <code>fetchResults</code> will only be called after 500 milliseconds of inactivity.</p><p><a name="throttle"></a></p><h3 id="throttle" tabindex="-1"><a class="header-anchor" href="#throttle" aria-hidden="true">#</a> .throttle</h3><p><code>.throttle</code> is similar to <code>.debounce</code> except it will release a handler call every 250 milliseconds instead of deferring it indefinitely.</p><p>This is useful for cases where there may be repeated and prolonged event firing and using <code>.debounce</code> won&#39;t work because you want to still handle the event every so often.</p><p>For example:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @scroll.window.throttle=&quot;handleScroll&quot;&gt;...&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The above example is a great use case of throttling. Without <code>.throttle</code>, the <code>handleScroll</code> method would be fired hundreds of times as the user scrolls down a page. This can really slow down a site. By adding <code>.throttle</code>, we are ensuring that <code>handleScroll</code> only gets called every 250 milliseconds.</p><blockquote><p>Fun Fact: This exact strategy is used on this very documentation site to update the currently highlighted section in the right sidebar.</p></blockquote><p>Just like with <code>.debounce</code>, you can add a custom duration to your throttled event:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @scroll.window.throttle.750ms=&quot;handleScroll&quot;&gt;...&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Now, <code>handleScroll</code> will only be called every 750 milliseconds.</p><p><a name="self"></a></p><h3 id="self" tabindex="-1"><a class="header-anchor" href="#self" aria-hidden="true">#</a> .self</h3><p>By adding <code>.self</code> to an event listener, you are ensuring that the event originated on the element it is declared on, and not from a child element.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click.self=&quot;handleClick&quot;&gt;
    Click Me

    &lt;img src=&quot;...&quot;&gt;
&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the above example, we have an <code>&lt;img&gt;</code> tag inside the <code>&lt;button&gt;</code> tag. Normally, any click originating within the <code>&lt;button&gt;</code> element (like on <code>&lt;img&gt;</code> for example), would be picked up by a <code>@click</code> listener on the button.</p><p>However, in this case, because we&#39;ve added a <code>.self</code>, only clicking the button itself will call <code>handleClick</code>. Only clicks originating on the <code>&lt;img&gt;</code> element will not be handled.</p><p><a name="camel"></a></p><h3 id="camel" tabindex="-1"><a class="header-anchor" href="#camel" aria-hidden="true">#</a> .camel</h3><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @custom-event.camel=&quot;handleCustomEvent&quot;&gt;
    ...
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Sometimes you may want to listen for camelCased events such as <code>customEvent</code> in our example. Because camelCasing inside HTML attributes is not supported, adding the <code>.camel</code> modifier is necessary for Alpine to camelCase the event name internally.</p><p>By adding <code>.camel</code> in the above example, Alpine is now listening for <code>customEvent</code> instead of <code>custom-event</code>.</p><p><a name="dot"></a></p><h3 id="dot" tabindex="-1"><a class="header-anchor" href="#dot" aria-hidden="true">#</a> .dot</h3><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @custom-event.dot=&quot;handleCustomEvent&quot;&gt;
    ...
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Similar to the <code>.camelCase</code> modifier there may be situations where you want to listen for events that have dots in their name (like <code>custom.event</code>). Since dots within the event name are reserved by Alpine you need to write them with dashes and add the <code>.dot</code> modifier.</p><p>In the code example above <code>custom-event.dot</code> will correspond to the event name <code>custom.event</code>.</p><p><a name="passive"></a></p><h3 id="passive" tabindex="-1"><a class="header-anchor" href="#passive" aria-hidden="true">#</a> .passive</h3><p>Browsers optimize scrolling on pages to be fast and smooth even when JavaScript is being executed on the page. However, improperly implemented touch and wheel listeners can block this optimization and cause poor site performance.</p><p>If you are listening for touch events, it&#39;s important to add <code>.passive</code> to your listeners to not block scroll performance.</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @touchstart.passive=&quot;...&quot;&gt;...&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,87),b={href:"https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#improving_scrolling_performance_with_passive_listeners",target:"_blank",rel:"noopener noreferrer"},g=t("\u2192 Read more about passive listeners");function f(y,w){const n=d("ExternalLinkIcon");return l(),s("div",null,[r,e("p",null,[u,e("a",p,[h,i(n)]),v]),m,e("p",null,[e("a",b,[g,i(n)])])])}var q=a(c,[["render",f],["__file","on.html.vue"]]);export{q as default};
