import{_ as e}from"./plugin-vue_export-helper.21dcd24c.js";import{o as n,c as t,g as i}from"./app.674943b1.js";const o={},a=i(`<h1 id="events" tabindex="-1"><a class="header-anchor" href="#events" aria-hidden="true">#</a> Events</h1><p>Alpine makes it simple to listen for browser events and react to them.</p><p><a name="listening-for-simple-events"></a></p><h2 id="listening-for-simple-events" tabindex="-1"><a class="header-anchor" href="#listening-for-simple-events" aria-hidden="true">#</a> Listening for simple events</h2><p>By using <code>x-on</code>, you can listen for browser events that are dispatched on or within an element.</p><p>Here&#39;s a basic example of listening for a click on a button:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button x-on:click=&quot;console.log(&#39;clicked&#39;)&quot;&gt;...&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>As an alternative, you can use the event shorthand syntax if you prefer: <code>@</code>. Here&#39;s the same example as before, but using the shorthand syntax (which we&#39;ll be using from now on):</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click=&quot;...&quot;&gt;...&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In addition to <code>click</code>, you can listen for any browser event by name. For example: <code>@mouseenter</code>, <code>@keyup</code>, etc... are all valid syntax.</p><p><a name="listening-for-specific-keys"></a></p><h2 id="listening-for-specific-keys" tabindex="-1"><a class="header-anchor" href="#listening-for-specific-keys" aria-hidden="true">#</a> Listening for specific keys</h2><p>Let&#39;s say you wanted to listen for the <code>enter</code> key to be pressed inside an <code>&lt;input&gt;</code> element. Alpine makes this easy by adding the <code>.enter</code> like so:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input @keyup.enter=&quot;...&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can even combine key modifiers to listen for key combinations like pressing <code>enter</code> while holding <code>shift</code>:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;input @keyup.shift.enter=&quot;...&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="preventing-default"></a></p><h2 id="preventing-default" tabindex="-1"><a class="header-anchor" href="#preventing-default" aria-hidden="true">#</a> Preventing default</h2><p>When reacting to browser events, it is often necessary to &quot;prevent default&quot; (prevent the default behavior of the browser event).</p><p>For example, if you want to listen for a form submission but prevent the browser from submitting a form request, you can use <code>.prevent</code>:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;form @submit.prevent=&quot;...&quot;&gt;...&lt;/form&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You can also apply <code>.stop</code> to achieve the equivalent of <code>event.stopPropagation()</code>.</p><p><a name="accessing-the-event-object"></a></p><h2 id="accessing-the-event-object" tabindex="-1"><a class="header-anchor" href="#accessing-the-event-object" aria-hidden="true">#</a> Accessing the event object</h2><p>Sometimes you may want to access the native browser event object inside your own code. To make this easy, Alpine automatically injects an <code>$event</code> magic variable:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;button @click=&quot;$event.target.remove()&quot;&gt;Remove Me&lt;/button&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="dispatching-custom-events"></a></p><h2 id="dispatching-custom-events" tabindex="-1"><a class="header-anchor" href="#dispatching-custom-events" aria-hidden="true">#</a> Dispatching custom events</h2><p>In addition to listening for browser events, you can dispatch them as well. This is extremely useful for communicating with other Alpine components or triggering events in tools outside of Alpine itself.</p><p>Alpine exposes a magic helper called <code>$dispatch</code> for this:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div @foo=&quot;console.log(&#39;foo was dispatched&#39;)&quot;&gt;
    &lt;button @click=&quot;$dispatch(&#39;foo&#39;)&quot;&gt;&lt;/button&gt;
&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>As you can see, when the button is clicked, Alpine will dispatch a browser event called &quot;foo&quot;, and our <code>@foo</code> listener on the <code>&lt;div&gt;</code> will pick it up and react to it.</p><p><a name="listening-for-events-on-window"></a></p><h2 id="listening-for-events-on-window" tabindex="-1"><a class="header-anchor" href="#listening-for-events-on-window" aria-hidden="true">#</a> Listening for events on window</h2><p>Because of the nature of events in the browser, it is sometimes useful to listen to events on the top-level window object.</p><p>This allows you to communicate across components completely like the following example:</p><div class="language-alpine ext-alpine line-numbers-mode"><pre class="language-alpine"><code>&lt;div x-data&gt;
    &lt;button @click=&quot;$dispatch(&#39;foo&#39;)&quot;&gt;&lt;/button&gt;
&lt;/div&gt;

&lt;div x-data @foo.window=&quot;console.log(&#39;foo was dispatched&#39;)&quot;&gt;...&lt;/div&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In the above example, if we click the button in the first component, Alpine will dispatch the &quot;foo&quot; event. Because of the way events work in the browser, they &quot;bubble&quot; up through parent elements all the way to the top-level &quot;window&quot;.</p><p>Now, because in our second component we are listening for &quot;foo&quot; on the window (with <code>.window</code>), when the button is clicked, this listener will pick it up and log the &quot;foo was dispatched&quot; message.</p><p><a href="./directives/on">\u2192 Read more about x-on</a></p>`,40),s=[a];function l(d,c){return n(),t("div",null,s)}var p=e(o,[["render",l],["__file","events.html.vue"]]);export{p as default};
